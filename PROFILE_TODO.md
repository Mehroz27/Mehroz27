# Profile TODO

GitHub username (`Mehroz27`), email, LinkedIn, and the HVAC live demo link are already wired into `README.md`.
What's left:

## Featured Systems section

- [ ] Add a repo/demo link to `[View Project →](#)` on the **AI Newsletter App** card
- [ ] Tighten the AI Newsletter App description to exactly what it does (content generation vs. curation vs. sending) — currently a reasonable placeholder description
- [ ] Confirm/replace the backend & DB in the Newsletter App's tech stack line (marked with a `<!-- REPLACE -->` comment in the file)
- [ ] Optionally add a screenshot/demo GIF to any card (drop into `/assets` and reference with `<img src="./assets/your-file.png" width="100%" />` inside the card)
- [ ] If you build/showcase more public projects later (or want the Marketing voice agent back in once you have a link for it), add more cards following the same `<table>` pattern

## GitHub Actions (contribution snake)

- [ ] Confirm **Settings → Actions → General → Workflow permissions** is set to "Read and write permissions" (required for the workflow to push to the `output` branch) — the snake is already live and running on a schedule
- [ ] GitHub Stats / Top Languages cards were removed for now (the public github-readme-stats.vercel.app instance kept 503'ing) — let me know if you want to add them back later, ideally via a self-hosted instance for reliability

## Optional polish

- [ ] No portfolio link is included (removed per your request) — add one back to the hero/Contact sections if you launch one later
- [ ] Swap any shields.io badge colors/icons in the Tech Stack section if you want a different accent than the indigo used throughout
